import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shubham.app',
  appName: 'Gio location',
  webDir: 'dist/JioLocation/browser'
};

export default config;
